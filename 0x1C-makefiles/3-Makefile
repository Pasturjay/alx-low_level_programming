RM = rm -f
CC = gcc
SRC = main.c school.c
OBJ = ${SRC:%.c=%.o}
NAME = school

%.0: %.c m.h
	${CC} -c -o $@ $<

all: ${OBJ}
	${CC} ${OBJ} -o ${NAME}

clean:
	${RM} *~ ${NAME}

oclean:
	${RM} ${OBJ}

fclean: 
	${RM} *~ ${NAME}
	${RM} ${OBJ}

re: oclean all
	
